# bybit-api-trading-guide
# 📈 바이비트(Bybit) API 트레이딩 스타터 킷 (2025)

이 저장소(Repository)는 암호화폐 퀀트 투자 및 자동매매(Auto Trading) 입문자를 위해 작성되었습니다.
안전한 API 키 발급 방법과 수익률을 갉아먹는 **거래 수수료(Transaction Fee)를 최소화하는 환경 설정**을 다룹니다.

## 🚀 1. 시작하기 전 필수 준비사항 (Prerequisites)

자동매매 로직보다 중요한 것은 **손익비 관리**입니다.
API 트레이딩은 매매 횟수가 많으므로, 수수료 할인이 적용되지 않은 계정을 사용하면 알고리즘이 우수해도 계좌는 우하향하게 됩니다.

따라서 반드시 **공식 파트너 레퍼럴(수수료 20% 할인 + 리베이트)**이 적용된 계정을 먼저 준비해야 합니다.

* **필수 계정 세팅:** [바이비트 수수료 20% 할인 적용 및 API 계정 생성 가이드]
* **주의:** 기존 계정이 있더라도 KYC 이전이라면 위 가이드를 통해 재설정이 가능합니다.

---

## 🛠 2. API Key 발급 및 보안 설정

1.  바이비트 로그인 후 **[My Profile]** -> **[API Management]** 이동
2.  **[Create New Key]** 클릭
3.  **System Generated API Keys** 선택
4.  권한 설정:
    * `Read-Write` 체크
    * `Orders`, `Positions` 체크
    * **주의:** `Withdraw` (출금) 권한은 절대 체크하지 마십시오. (해킹 방지)

더 상세한 **[한글 메뉴얼 및 이미지 가이드]**는 아래 링크를 참고하세요.
👉 [API 키 발급 따라하기 (초보자용)]

---

## 💻 3. 테스트 코드 예제 (Python)

계정 세팅이 완료되었다면, 아래 파이썬 코드로 현재 비트코인 가격을 조회해볼 수 있습니다.

```python
import requests

def get_btc_price():
    url = "[https://api.bybit.com/v5/market/tickers](https://api.bybit.com/v5/market/tickers)"
    params = {'category': 'linear', 'symbol': 'BTCUSDT'}
    response = requests.get(url, params=params)
    data = response.json()
    price = data['result']['list'][0]['lastPrice']
    print(f"현재 비트코인 가격: ${price}")

if __name__ == "__main__":
    get_btc_price()
